## What & why

<!-- What does this change, and why? Link an issue if there is one. -->

## Testing

<!-- `uv run pytest`, plus anything you checked manually (e.g. --library dummy runs). -->

- [ ] `uv run ruff check . && uv run ruff format --check . && uv run mypy src && uv run pytest` all pass
